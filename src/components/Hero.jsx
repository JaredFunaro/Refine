import { styles } from '../styles.js'
import { blackLogo, mansion } from '../assets'
const Hero = () => {
  return (
    <section className={`relative flex flex-col justify-center w-full h-screen mx-auto`}>
      <div className={``}>
      </div>
    </section>
  )
}

export default Hero